# Experiments

This directory is for small technical probes before code is promoted into the main Tauri app.

Initial topics:

- Windows keyboard input detection and injection
- SendInput timing and repeat-rate measurements
- Foreground-window checks for DNF-specific behavior
- Rust/Tauri API shape experiments

Keep experiments isolated and document the result before moving code into `src-tauri` or `src`.
